var RailsPost = React.createClass({
    render() {
      console.log("this.props.cmember")
      console.log(this.props.cmember)
      return (
        <div>
          {/* <img src={this.props.cmember.face_photo_path.url} style={{width:"50px"}}/> */}
        </div>
      );
    }
  });
  